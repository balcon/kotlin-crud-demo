spring:
  datasource:
    url: jdbc:postgresql://localhost:5436/postgres
    username: postgres
    password: postgres
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
    open-in-view: false
  output.ansi.enabled: always

logging:
  level:
    liquibase.executor: debug
